// Variables that needs to be accessed globally

// Keeps track of medias the user liked
export const likedMediaList = []

// Array containing the info and medias of currently displayed photographer
export let displayedPhotographerData = { photographer: null, media: null }
